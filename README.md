# lista007-Java-Script
O foco é em Matrizes (Arrays de Arrays, e.g., $3 \times 3$, $2 \times 2$). A principal técnica é o uso de loops aninhados (um for para as linhas e outro para as colunas) para acessar e manipular elementos por coordenadas ($i$, $j$).
